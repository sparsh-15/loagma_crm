<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Development-only: allow cleartext (HTTP) for all domains.
      For production build, remove or scope this to specific domains.
    -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
